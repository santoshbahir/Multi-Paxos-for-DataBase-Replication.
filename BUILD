Build the project and create necessary dirctoried change to
the project directory and build project with following command.

	make directories paxos gendataset

This command creates input, output and log directory in current directory.
